% Plot \Gamma_1D/\Gamma^prime for groupmt 20171115

clear; clc;
%%
Width = 10:10:50;
gp = 6:10:26;
Ratio = zeros(length())

for kw = 1:length(Width)    
    load(['Xeven_W',num2str(Width(kw)),'_gp']);
    ke_even = fit_para(:,3);
    ki = fit_para(:,1);
    load(['Xodd_W',num2str(Width(kw)),'_gp']);
    ke_odd = fit_para(:,3);
    
end